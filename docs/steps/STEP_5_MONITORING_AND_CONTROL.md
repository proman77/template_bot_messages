Вот подробная спецификация для **Этапа 5**. Она сфокусирована на том, чтобы дать вам полный контроль над процессом рассылки и защитить бота от блокировки Telegram.

***

# STEP_5_MONITORING_AND_CONTROL.md: Управление, Мониторинг и Лимиты — ✅ ВЫПОЛНЕНО

На этом этапе мы превратили "слепую" отправку сообщений в управляемый процесс. Мы внедрили "Панель управления" (Control Plane), которая работает в реальном времени через Redis, и механизм защиты от превышения лимитов API (Rate Limiting).

## 🎯 Цели этапа
1.  **Live-Статистика:** Реализовать отображение прогресса рассылки (отправлено / ошибок / осталось) в админке с обновлением "на лету". — ✅ **DONE**
2.  **Управление:** Реализовать кнопки **"Пауза"**, **"Возобновить"** и **"Стоп"**, которые мгновенно влияют на работу воркеров. — ✅ **DONE**
3.  **Rate Limiting:** Внедрить глобальное ограничение скорости отправки (Distributed Rate Limiter) для защиты от ошибок `429 Too Many Requests`. — ✅ **DONE**

---

## 1. Архитектура Данных (Redis Schema) — ✅ ВЫПОЛНЕНО

PostgreSQL слишком медленный для инкремента счетчиков 50-100 раз в секунду. Мы используем Redis как "горячее" хранилище состояния текущей рассылки.

### 1.1. Ключи Статистики
Используем структуру `Hash` или набор ключей. TTL ключей = 48 часов.

*   `broadcast:{camp_id}:total` — (int) Общее количество пользователей (устанавливает **Loader**).
*   `broadcast:{camp_id}:sent` — (int) Успешно отправлено (инкрементит **Sender**).
*   `broadcast:{camp_id}:fail` — (int) Ошибки отправки (инкрементит **Sender**).

### 1.2. Ключ Управления
*   `broadcast:{camp_id}:status` — (str) Текущее состояние.
    *   Возможные значения: `sending`, `paused`, `stopped`, `completed`.
    *   По умолчанию (после Loader): `sending`.

---

## 2. Модернизация Воркера (`Sender Task`) — ✅ ВЫПОЛНЕНО

Текущая задача `send_one_message` стала умнее.

### 2.1. Логика проверки статуса
Перед любой сетевой активностью задача проверяет ключ `broadcast:{camp_id}:status`.

1.  **Если `sending`:** Продолжаем работу.
2.  **Если `paused`:**
    *   Входит в цикл ожидания с `asyncio.sleep(5)`.
    *   *Результат:* Задача не завершается, а ждет смены статуса на `sending` или `stopped`.
3.  **Если `stopped`:**
    *   Молча завершает задачу (`return`).

### 2.2. Обновление счетчиков
После попытки отправки:
*   Если успех: `await redis.incr(f"broadcast:{camp_id}:sent")`.
*   Если ошибка: `await redis.incr(f"broadcast:{camp_id}:fail")`.

---

## 3. Global Rate Limiter (Taskiq Middleware) — ✅ ВЫПОЛНЕНО

Реализован распределенный лимитер через Middleware.

### 3.1. Алгоритм
1.  Определяет ключ лимита: `limiter:broadcast`.
2.  Проверит текущее значение в Redis.
3.  Если лимит превышен — засыпает на 0.1с и пробует снова.
4.  Если лимит позволяет — инкрементит счетчик с TTL 1 сек.

---

## 4. Интерфейс Админа (Aiogram-dialog Update) — ✅ ВЫПОЛНЕНО

Обновлено окно статуса рассылки.

### 4.1. Визуализация
Добавлен геттер `get_monitoring_data`, который формирует текст с прогресс-баром: `[████░░░░░░] 40%`.

### 4.2. Кнопки управления
1.  **🔄 Обновить:** Обновляет данные из Redis.
2.  **⏸ Пауза / ▶️ Продолжить:** Мгновенно меняет статус в Redis, воркеры реагируют через цикл ожидания.
3.  **⏹ Стоп:** Останавливает рассылку и закрывает диалог.

---

## 5. Завершение Рассылки — ✅ ВЫПОЛНЕНО
*   Когда `Отправлено + Ошибок == Всего`, появляется кнопка "Завершить".
*   Кнопка закрывает диалог, кампания в БД остается в статусе `completed`.
*   **Fix:** Добавлена принудительная установка статуса `completed` в Redis по завершении работы Loader-а.

---

## 6. Итоги Тестирования и Исправления — ✅ ВЫПОЛНЕНО

В ходе финального тестирования были выявлены и исправлены следующие проблемы:
1.  **Race Condition (Статус unknown):** Исправлено путем мгновенной инициализации ключей в Redis на стороне бота перед запуском воркера.
2.  **Missing Imports:** Исправлен `NameError: logger` в файле обработчиков.
3.  **Redis Configuration:** Включен `USE_CACHE = true` в `settings.toml` для корректной работы лимитера и мониторинга.
4.  **Language Filtering:** Подтверждена корректная работа фильтрации по языку (сообщения уходят только подходящим пользователям).
5.  **Windows Loop Fix:** Применен патч для `SelectorEventLoop` в воркере для стабильной работы под Windows.

---

## ✅ Критерии Приемки (Definition of Done)

1.  [x] **Rate Limiter:** Скорость стабильна (~25 msg/sec), ошибок 429 нет.
2.  [x] **Live Stats:** В Redis инкрементируются счетчики `sent` и `fail`.
3.  [x] **Пауза:**
    *   [x] Нажатие "Пауза" -> Воркеры входят в режим ожидания.
    *   [x] Нажатие "Продолжить" -> Воркеры продолжают отправку.
4.  [x] **Стоп:** Нажатие "Стоп" прекращает рассылку навсегда.
5.  [x] **Restart-устойчивость:** Статистика в Redis сохраняется при перезагрузке воркеров.

---
**Этап 5 полностью завершен и протестирован.** Система готова к промышленной эксплуатации.